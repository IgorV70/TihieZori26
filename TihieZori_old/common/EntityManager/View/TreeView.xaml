<UserControl x:Class="EntityManager.View.TreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <TreeView x:Name="tvMain" ItemsSource="{Binding Root}" BorderThickness="0" SelectedItemChanged="TvMain_OnSelectedItemChanged" Margin="5" >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" ContextMenu="{Binding CntMenu}">
                        <Image Source="{Binding ImagePath}" MaxHeight="25" MaxWidth="25"/>
                        <TextBlock VerticalAlignment="Center" Text="{Binding SubformObject.Name}" />
                        <StackPanel.Style>
                            <Style>
                                <Style.Triggers>
                                    <Trigger Property="StackPanel.IsMouseOver" Value="True">
                                        <Setter Property="StackPanel.Background" Value="Cyan" />
                                    </Trigger>
                                    <DataTrigger Binding="{Binding IsSelected}" Value="true">
                                        <Setter Property="StackPanel.Background" Value="Blue"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </ScrollViewer>
</UserControl>
